#include <iostream>
#include <string>
#include <vector>

#include "stack.h"

/*
template<typename T>
Stack<T>::Stack() {

}

template<typename T>
Stack<T>::~Stack() {
	for (std::vector<Entity*> v : stackVector) {//comment out
		for (Entity* e : v) {
			delete e;
		}
	}
}

template<typename T>
std::vector<T> Stack<T>::getStack() {
	return stack;
}
*/